<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper 
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="premier.xml.player">
	<sql id="allPlayer">
		SELECT RM, PLAYERNO, LEAGUENAME, TEAMNAME, BACKNUMBER, FIRSTNAME, 
			LASTNAME, NATIONALITY, TO_CHAR(BIRTHDATE, 'YYYY/MM/DD') BIRTHDATE, HEIGHT, WEIGHT, 
			BODYSIZE, FAVORITEPOSITION, PLAYSTYLE, MAINFOOT, WEAKFOOTABILITY, LEADERSHIP, 
			POSITIONSTAT, RECOMMENDEDPOSITION, POTENTIAL, TOTALSTAT, STAMINA, PHYSICAL, 
			COMPOSURE, LONGRANGESHOT, SHOOTINGACCURACY, SHOOTINGPOWER, FREEKICK, PACE, 
			ACCELERATION, DRIBBLE, LONGPASS, CROSS, PASS, REACTION, INDIVIDUALSKILL, TACKLE, 
			MARKING, HEADING, POSITIVENESS, HANDLING, REFLEXES, DIVING, POSITIONING 
		FROM (
			SELECT ROW_NUMBER() OVER(ORDER BY PLAYERNO) RM, PLAYERNO, LEAGUENAME, TEAMNAME, BACKNUMBER, 
				FIRSTNAME, LASTNAME, NATIONALITY, BIRTHDATE, HEIGHT, WEIGHT, 
				BODYSIZE, FAVORITEPOSITION, PLAYSTYLE, MAINFOOT, WEAKFOOTABILITY, LEADERSHIP, 
				POSITIONSTAT, RECOMMENDEDPOSITION, POTENTIAL, TOTALSTAT, STAMINA, PHYSICAL, 
				COMPOSURE, LONGRANGESHOT, SHOOTINGACCURACY, SHOOTINGPOWER, FREEKICK, PACE, 
				ACCELERATION, DRIBBLE, LONGPASS, CROSS, PASS, REACTION, INDIVIDUALSKILL, TACKLE, 
				MARKING, HEADING, POSITIVENESS, HANDLING, REFLEXES, DIVING, POSITIONING
			FROM PLAYER
		)
	</sql>
	<select id="selectAllPlayer" parameterType="java.util.HashMap" resultType="player">
		<include refid="allPlayer"/>
		WHERE RM BETWEEN #{start} AND #{end}
	</select>
	<select id="selectOnePlayer" parameterType="java.lang.String" resultType="player">
		<include refid="allPlayer"/>
		<where>
			PLAYERNO = #{playerNo}
		</where>
	</select>
	<select id="getTotalRow" resultType="java.lang.Integer">
		SELECT COUNT(PLAYERNO)
		FROM PLAYER
	</select>
</mapper>